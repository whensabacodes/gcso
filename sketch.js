var bluerect,redrect;


function setup() {
  createCanvas(800,400);
  // createSprite(400, 200, 50, 50);
  bluerect=createSprite(200,200,50,100);
  bluerect.shapeColor="blue";

}

function draw() {
  background("lightgreen");  
  redrect.x=World.mouseX;
  redrect.y=World.mouseY;

  if(redrect.)
  drawSprites();
}